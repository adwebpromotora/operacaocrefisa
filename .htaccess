# .htaccess na raiz do projeto

RewriteEngine On

# Redireciona tudo para a pasta public/
RewriteRule ^(.*)$ public/$1 [L]

# Ou versão mais segura (evita loop infinito)
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]
